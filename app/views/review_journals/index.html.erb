<h1>Listing journals</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Link</th>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @review_journals.each do |journal| %>
  <tr>
    <td><%= journal.name %></td>
    <td><%= journal.link %></td>
    <td><%= journal.description %></td>
    <td><%= link_to 'Show', journal %></td>
    <td><%= link_to 'Edit', edit_review_journal_path(journal) %></td>
    <td><%= link_to 'Destroy', journal, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Journal', new_review_journal_path %>
